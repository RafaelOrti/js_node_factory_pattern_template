# .npmrc

# Especificar el registro npm para el proyecto
registry=https://registry.npmjs.org/

# Habilitar la instalación en modo producción por defecto
production=true

# Ignorar los scripts durante la instalación (útil en entornos de producción)
ignore-scripts=true

# Utilizar un espejo de npm para acelerar las descargas
prefer-offline=true
prefer-online=false
offline=true

# Especificar un tiempo de espera para conexiones de red en milisegundos
timeout=30000

# Utilizar siempre HTTPS para las conexiones de npm
strict-ssl=true

# Incluir sólo las dependencias necesarias al publicar el paquete
production=true

# Deshabilitar la telemetría para respetar la privacidad del usuario
metrics=false
