// index.ts
// This file serves as an entry point to the scripts folder.

// Import and export the scripts you want to make available as modules here.

// Example: Exporting the setup script
export { default as setup } from './setup';

// Example: Exporting the app script
export { default as app } from './app';

// Example: Exporting the AnotherService script
export { default as anotherService } from './anotherService';

// Example: Exporting the ProductService script
export { default as productService } from './productService';

// Example: Exporting the productRoutes script
export { default as productRoutes } from './productRoutes';
